# when thsi entire application is started, what exactly commands should run 
# And the commands here we use are something related to Gunicorn(Green unicorn)
# Gunicorn - pure http server for wsgi applications, it allows u to run python applications 
# concurrently by running multiple processes

web: gunicorn app:app